[package]
name = "client"
description = "Chaos endpoint agent: collects telemetry, detects locally, and ships to the server."
edition.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
rust-version.workspace = true
version.workspace = true

[[bin]]
name = "cclient"
path = "src/main.rs"

[dependencies]
mimalloc.workspace = true
model.workspace = true
pipeline.workspace = true
ports.workspace = true
protocol.workspace = true
sigma.workspace = true
supervision.workspace = true
transport.workspace = true
# The egress queue. The writer thread is what keeps the network off the
# collection loop's path, so this is a dependency of correctness, not taste.
crossbeam-channel.workspace = true

chrono.workspace = true

[target.'cfg(windows)'.dependencies]
etw.workspace = true
observer.workspace = true
respond.workspace = true
