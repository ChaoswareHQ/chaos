[package]
name = "protocol"
description = "The client/server wire contract: enrollment, batched ingest, and host-token format."
edition.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
chrono.workspace = true
# The body encoding belongs to the wire contract, not to either side of it: a
# client that compresses and a server that does not is a protocol mismatch, and
# keeping both halves here is what makes that impossible.
flate2.workspace = true
model.workspace = true
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
