[package]
name = "pipeline"
description = "Wires the ASMR axioms to the ETW sensor: decode, project state, score, decide, govern, alert."
edition.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
asmr.workspace = true
chrono.workspace = true
model.workspace = true
ports.workspace = true
sigma.workspace = true

[dev-dependencies]
# Only the tests need a clock; the engine itself takes timestamps from the
# events it is handed, which is what keeps it deterministic.
chrono.workspace = true
