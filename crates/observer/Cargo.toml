[package]
name = "observer"
description = "The observation runtime: drives a source, a scorer and a sink as one loop."
edition.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
model.workspace = true
ports.workspace = true

[dev-dependencies]
# The runtime's tests build synthetic wire events, and a wire event carries a
# timestamp. Nothing in the library reads a clock, so this is test-only.
chrono.workspace = true
